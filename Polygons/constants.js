// Define the Point Class
class Point {
    constructor(x, y) {
      this.x = x;
      this.y = y;
      this.color = "black"; // Default color
    }
}

const COLORS = ["red", "green", "blue"]; // Red, Green, Blue